# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Configuration
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
email-validator==2.2.0

# Auth
passlib[argon2]==1.7.4
python-jose[cryptography]==3.3.0
itsdangerous==2.2.0

# Search (Typesense client)
typesense==0.21.0

# HTTP / external APIs
httpx==0.28.1
aiofiles==24.1.0

# FTP for FACS push
aioftp==0.23.1

# CSV (stdlib is enough but pandas useful for data work)
pandas==2.2.3

# XLSX reader — used by scripts/import_pace_flatfile.py to ingest AWDA PIES
# flat-file brand exports delivered as .xlsx (CSV exports need no extra dep).
openpyxl==3.1.5

# Email
aiosmtplib==3.0.2
jinja2==3.1.5

# Background jobs / scheduling
apscheduler==3.11.0

# Server-side transcription of Issue Recorder .webm audio (CPU; ctranslate2 +
# bundled PyAV/ffmpeg — no torch, no system ffmpeg needed). See
# app/services/transcription.py + the /api/error-reports/{id}/transcribe route.
faster-whisper==1.0.3

# Money + date handling
python-dateutil==2.9.0

# MySQL client (for one-time WSM migration only — not runtime dep)
PyMySQL==1.1.1

# Async MySQL client — used by customer_sync.py to pull tte_cus190 from
# Titan's online MySQL into our local Postgres customer + customer_address tables.
aiomysql==0.3.2

# HEIC/HEIF decoder used by the RMA router to transcode iPhone photos to
# JPEG before attaching them to the sales@titantruck.com email. Optional
# at runtime — if missing, HEIC uploads return a clear 400.
pillow-heif==1.3.0
Pillow>=11.0.0

# Anthropic SDK (Phase 2 AI features; installed but unused in Phase 1)
anthropic==0.43.0

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.25.2
ruff==0.9.1
